- AIPWE not good performance
    --> try implementing weights as in cc-learning paper: just repeat every instance (X) for every treatment level, then make the label every treatment level, then apply weights
    - caveat: research what you want as high/low weight (optimal action high or low weight?)
    - caveat: is this necessary for binary?
    IMPLEMENTED --> maybe double check
- then there must be something wrong with either ps or outcome model --> try to check optimal actions and contrast functions
    - try from sklearn.calibration import CalibratedClassifierCV
    IMPLEMENTED (is easier)

- try delete variables second point!! for ps
    - interest rate: keep column 0, 1, 9, 10 (est_quality, elapsed time, cum_cost, amount)
    - choose procedure: delete last column (column 2) (only keep amount, est_quality)
    IMPLEMENTED

- try out not doubly robust, but t-learner, or x-learner, or only pr model type stuff --> not really the problem

- problem: ps model 
    - what is wrong? too large variance --> but it has very good f1 score? 
    - maybe try again with 50 000?? 
    - Or maybe calibrate even more? --> nope
    - Or something wrong with formula? --> Note: if I do only ps model (so IPW learner) I get better than combination, but still not like only outcome model
        - if I do combination, but with "correct" ps (so 0.9333 and 0.03333 for delta 0.9) then around 200, so even more close, but still not like only outcome model
    --> Just try out causal forest

- try out g-computation? (one model per step, start from back, target for previous model is predictions of model)
    - target = previous predictions
    - features: history + future treatments

- NOTE: Custom loss function for XGB_Opt_Class seems to work fine (better than when doing for f1 score for example)

- Maybe try out cross-fitting for dtr-reg as well (over the different decision points)




- for 500: data not necessarily has all treatments for second intervention... --> edit data generation






- Quick todo's:
    - check why DR-M == DR-R --> just very similar I guess? NOT SURE
    - implement all with econml DONE
    - cross-fit dtr-reg? --> DID NOT REALLY IMPROVE; TO CHECK WHETHER correct
    - try out with 20 000, see if the bigger the dataset the better
    - try out with 1000
    - train/tune branchi correctly
    - try out classification instead of regression for DR-learner DONE
    - check for S-learner whether Treatment gets added in the 'correct' way DONE
    - RA/DR --> tune outcome separately! --> for example, just take from s-learner/t-learner, then tune cate/class DONE
    IMPORTANT
    - RA: we now get q-values for Regret based on outcome only, when we might want to have it based on CATE (effect); but we SHOULD keep it on outcome only when calculating pseudo-outcome (of course, because we debias then already using e.g. X-learner)

    THIS DIDN't WORK IMMEDIATELY --> NOT PRIORITY
    - try out instance-dependent cost-sensitive loss function (now we use 'weighted' actually)
    - try out instance dependent cost-senstive thresholding --> now thresholds are not saved correctly I think DID NOT WORK THAT WELL, DON'T KNOW WHY

- PRIORITIES:
    - use DL instead of ML (also try out with weights in loss function)
    - KMEANS CLUSTERING
    - experiment with one decision point RCT, other bank
    - even lower data size
    - more granular confounding at the end
    - possibly X-learner again, because we didn't take the "effect" for the regret function but the (biased, just s-learner) outcome model (describe above under IMPORTANT)
        - results op VSC zijn al met 'effect' --> als niet beter, rerun met 'outcome' voor q-value bij regret ipv effect
        - could be that this is theoretically not desirable though (they just use the 'outcome' in c-learning paper, but they do use the optimal causal estimate of course to get the optimally estimated action)
	- - exp with 50 000 were cancelled...; exp 10 000 and 20 000 are still to be downloaded
    - rerun everything again to be sure --> only the core! (1000, 10 000, 50 000; S, T, RA)


- if we do linear regression for x-learner --> 

- names of methods:
    dtr-learner-optimal-value_function
    e.g., dtr-S-reg-R, or dtr-DRSMultiTask-class-R, dtr-S-reg-G
    or 
    separate-S-reg-none

    init_model_params["learner_method"] = method.split("-")[1] if "dtr" in method else ""
    init_model_params["action_recomm_method"] = method.split("-")[2] if "dtr" in method else ""
    init_model_params["value_function_method"] = method.split("-")[-1] if "dtr" in method else ""

- errors:
    - 2000, 3000, 4000 at 0.99 for T-learner: no y_true/y_pred??




- TODO final:
    - finish simulation
    - adjust for multiple stages
    - run for multiple stages (also do evaluation the same?)
    - check LSTM: tensor encoding has 'missing values' --> check if we actually input -100 --> THERE ARE NO MISSING VALUES SO WE ARE SAFE (phew)

        # PAST THIS INTO MAIN TO CHECK

        # very quickly check whether there is any missing values in the preprocessed data (-100 is the missing value indicator)
        data_to_check = prepped_data_dict["train"]["tensor"][1]["X_event"]

        # Assuming data_to_check is your tensor
        missing_mask = (data_to_check == -100)

        # Check if there are any missing values
        has_missing = missing_mask.any()
        print("Any missing values?", has_missing.item())

        # (Optional) Count how many missing values there are
        num_missing = missing_mask.sum()
        print("Number of missing values:", num_missing.item())
    - TO RERUN: dtr-S-reg-R & separate-S-reg-none voor 10 000 van 0.9-0.99; also add to offline files

    - check whether: "elapsed_time_incomplete_files" is not used in prep, and delete it from existing unpreprocessed datasets